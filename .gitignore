*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/core/build
/app/build

# The order-page fixtures are screenshots of a real order: page three carries a card
# last four, the order number and a barcode. Keep them local, see the readme.
app/src/androidTest/assets/order-page-*.png

# Working directory for fixture captures and backups. Amazon Fresh order pages land
# here and two of them carry a card last four, so this must never be committable.
/scratchpad/
