<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      SPEC 4.10: the INTERNET permission exists in this product flavour and nowhere else.
      The default `standard` build cannot reach the network even in principle.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
